defaults:
  - _self_

output_root: /tmp/serotiny_${uuid:}
seed: 42

hydra:
  run:
    dir: ${output_root}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S_%f}
  sweep:
    dir: ${output_root}/${mode}/${now:%Y-%m-%d}
    subdir: ${now:%H-%M-%S_%f}
