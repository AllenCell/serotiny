datamodule:
  name: DummyDatamodule
  config:
    batch_size: 16
    num_workers: 2
    x_label: x
    y_label: y
    length: 64
    x_dims: [32]
    y_dims: [32]

model:
  name: TabularConditionalPriorVAE
  config:
    x_dim: 32
    c_dim: 32
    latent_dim: 8
    hidden_layers: [8]
    prior_encoder_hidden_layers: [8]
    x_label: x
    c_label: y
    optimizer: "Adam"
    lr: 0.001
